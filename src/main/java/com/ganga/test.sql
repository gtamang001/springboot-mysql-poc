-- Create Table SALES * note the caps matters
CREATE TABLE SALES (
  id int(11) NOT NULL AUTO_INCREMENT,
  item varchar(45) NOT NULL,
  quantity varchar(45) NOT NULL,
  amount float NOT NULL,
  PRIMARY KEY (id)
);